How To Use Regetron 

commands and options:

	-If you give it a file on the cli it will load that: regtron somefile.txt

	- If you hit ENTER to make a blank line it will go into 'verbose mode'. To finish 
	in verbose mode and run the regex, just enter a blank line. 

	- !data EXP will run any python expression (EXP) and set that as the data. 

	- !help will print available commands

	- !load FILE will laod the given file

	- !match toggles wheter regex are fun in match vs. search mode. 

	- use Ctrl-d to exit. 
